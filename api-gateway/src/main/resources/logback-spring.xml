<?xml version="1.0" encoding="UTF-8"?>

<configuration>

    <!-- Load Spring Boot's default Logback settings -->
    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>

    <!-- Read spring.application.name -->
    <springProperty
            scope="context"
            name="APP_NAME"
            source="spring.application.name"
            defaultValue="SHOPVERSE"/>

    <!-- Main application log file -->
    <springProperty
            scope="context"
            name="LOG_FILE"
            source="logging.file.name"
            defaultValue="logs/${APP_NAME}.log"/>

    <!-- Optional health log file -->
    <springProperty
            scope="context"
            name="HEALTH_LOG_FILE"
            source="shopverse.logging.health-file"
            defaultValue="logs/${APP_NAME}-health.log"/>

    <!-- Structured logging format (typically logstash JSON) -->
    <springProperty
            scope="context"
            name="STRUCTURED_FORMAT"
            source="logging.structured.format.file"
            defaultValue="logstash"/>

    <!-- Console logging -->
    <appender name="CONSOLE"
              class="ch.qos.logback.core.ConsoleAppender">

        <encoder class="org.springframework.boot.logging.logback.StructuredLogEncoder">
            <format>${STRUCTURED_FORMAT}</format>
        </encoder>

    </appender>

    <!-- File logging with rotation -->
    <appender name="APP_FILE"
              class="ch.qos.logback.core.rolling.RollingFileAppender">

        <!-- Active log file -->
        <file>${LOG_FILE}</file>

        <!-- Rotate logs by date and size -->
        <rollingPolicy
                class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">

            <!-- Archive naming pattern -->
            <fileNamePattern>
                ${LOG_FILE}.%d{yyyy-MM-dd}.%i.gz
            </fileNamePattern>

            <!-- Roll when file reaches 10 MB -->
            <maxFileSize>10MB</maxFileSize>

            <!-- Keep 7 days of archives -->
            <maxHistory>7</maxHistory>

            <!-- Total archive size limit -->
            <totalSizeCap>256MB</totalSizeCap>

        </rollingPolicy>

        <!-- JSON structured logs -->
        <encoder class="org.springframework.boot.logging.logback.StructuredLogEncoder">
            <format>${STRUCTURED_FORMAT}</format>
        </encoder>

    </appender>

    <!-- Root logger -->
    <root level="INFO">

        <!-- Write logs to console -->
        <appender-ref ref="CONSOLE"/>

        <!-- Write logs to file -->
        <appender-ref ref="APP_FILE"/>

    </root>

</configuration>