import type {McqConcept} from './types';

export const javaConcepts: readonly McqConcept[] = [
  ['equals-hashcode', '`equals()` and `hashCode()`', 'Equal objects must produce the same hash code, and both methods should use consistent identity fields.', 'Equal objects may return unrelated hash codes because buckets are checked with `equals()` later.', '`hashCode()` must be globally unique for every live object.', 'Overriding `equals()` alone is sufficient for hash-based collections.', 'Hash-based collections choose a bucket using the hash and then compare candidates. Breaking the equal-implies-same-hash contract can make a logically equal key unreachable.'],
  ['immutability', 'immutability', 'An immutable object prevents observable state changes after construction and should not expose mutable internals.', 'A class is immutable whenever all fields are declared private.', 'Returning an internal mutable list is safe when the field reference is final.', 'Immutability means the object can never reference another object.', 'Final references do not freeze referenced objects. Use validation, defensive copies, immutable components, and no mutating escape paths.'],
  ['exceptions', 'checked and unchecked exceptions', 'Checked exceptions are compile-time declared/caught; unchecked exceptions usually represent programming or unrecoverable contract failures, but API policy still matters.', 'Checked exceptions are always retryable and unchecked exceptions are never retryable.', 'The JVM handles unchecked exceptions without stack unwinding.', 'Catching `Exception` at every layer improves recovery.', 'Classification should express ownership and recovery. Preserve causes, translate at boundaries, and avoid broad catches that hide cancellation or programming defects.'],
  ['generics', 'generic invariance and wildcards', '`List<Integer>` is not a subtype of `List<Number>`; use bounded wildcards according to producer/consumer intent.', 'Java generics are covariant, so any subtype list can be assigned to a supertype list.', '`List<?>` accepts adding values of any reference type.', 'Type erasure makes generic compile-time checks optional.', 'Invariance prevents inserting an incompatible value. PECS is a useful guide: producers extend, consumers super.'],
  ['optional', '`Optional`', '`Optional` models a possibly absent return value; it is usually unsuitable for entity fields, method parameters, or serialization contracts.', '`Optional.get()` is the recommended default access pattern.', '`Optional` guarantees lazy execution of `orElse` arguments.', 'Wrapping every nullable field in `Optional` removes null handling costs.', '`orElse` evaluates eagerly while `orElseGet` is lazy. Use Optional at deliberate API boundaries, not as universal storage.'],
  ['streams', 'Java Streams', 'Intermediate operations are lazy and execute when a terminal operation consumes the pipeline.', 'A stream stores all transformed elements immediately after each intermediate call.', 'Parallel streams always preserve speed and remove thread-safety concerns.', 'A stream can be reused after a terminal operation.', 'Streams are single-use pipelines. Parallel execution adds splitting, merging, ordering, and shared-resource costs that must be measured.'],
  ['hashmap', '`HashMap` internals', '`HashMap` uses hash spreading and bucket lookup, then equality; collisions may use linked nodes or tree bins depending on conditions.', 'A hash collision overwrites the existing entry regardless of key equality.', '`HashMap` provides deterministic insertion-order iteration.', 'Concurrent writes are safe when keys are immutable.', 'Good key immutability and a correct equality contract matter. HashMap is not a concurrent collection and iteration order is not its contract.'],
  ['concurrent-map', '`ConcurrentHashMap`', '`ConcurrentHashMap` supports concurrent access without one global map lock and supplies atomic compound operations such as `compute`.', 'Its iterators throw `ConcurrentModificationException` for every concurrent write.', 'Calling `containsKey` followed by `put` is automatically one atomic operation.', 'It permits null keys and values to represent missing results.', 'Use atomic map methods for read-modify-write logic. Null is disallowed, and iterators are weakly consistent rather than fail-fast snapshots.'],
  ['lists', '`ArrayList` versus `LinkedList`', '`ArrayList` usually has better locality and random access; LinkedList only helps specific insertion patterns when a node position is already known.', '`LinkedList` gives O(1) insertion at an arbitrary index because it never traverses.', '`ArrayList` insertion is always O(1).', '`LinkedList` uses less memory per element.', 'Big-O alone misses traversal, allocation, pointer overhead, and cache locality. Measure representative workloads.'],
  ['happens-before', 'the happens-before relationship', 'Happens-before provides visibility and ordering guarantees between actions through rules such as locks, volatile access, thread start/join, and initialization.', 'It means two operations happened at different wall-clock times.', 'It is created by any method call between two threads.', 'It guarantees compound operations are atomic.', 'Without a happens-before edge, another thread may observe stale or partially published state even when execution seemed ordered in tests.'],
  ['volatile', '`volatile`', '`volatile` provides visibility and ordering for reads/writes of the variable but does not make compound read-modify-write operations atomic.', '`volatile int count++` is an atomic counter.', '`volatile` locks every method on the containing object.', '`volatile` makes referenced collections thread-safe.', 'Use volatile for independent state publication or flags. Use atomic types, locks, or confinement for compound invariants.'],
  ['synchronized', '`synchronized`', 'Entering and leaving the same monitor provides mutual exclusion and a happens-before relationship.', 'Synchronizing two different lock objects protects the same critical section.', '`synchronized` prevents deadlock by imposing a global order.', 'A synchronized method locks the current thread object.', 'Instance synchronization uses the receiver monitor; static synchronization uses the Class monitor. Lock scope and acquisition order remain design concerns.'],
  ['deadlock', 'deadlock prevention and diagnosis', 'Consistent lock ordering, bounded acquisition, reduced lock scope, and thread-dump evidence are central controls.', 'Increasing the thread pool resolves lock cycles.', 'Deadlock requires high CPU saturation.', 'Interrupting any arbitrary thread is always a safe recovery.', 'A deadlock is a wait cycle. Preserve dumps and lock ownership, contain safely, then correct ordering or resource acquisition.'],
  ['future', '`CompletableFuture`', 'Async stages use an executor; blocking joins, default common-pool use, exception flow, cancellation, and context propagation must be designed explicitly.', 'Every stage creates a new operating-system thread.', '`thenApply` and `thenApplyAsync` always execute identically.', 'Cancellation automatically interrupts all downstream work.', 'Async composition does not remove resource limits. Select executors and time budgets, and handle exceptional completion deliberately.'],
  ['virtual-threads', 'virtual threads', 'Virtual threads make thread-per-task blocking code cheaper, but database connections, sockets, CPU, locks, and downstream concurrency remain bounded.', 'They make CPU-bound work execute in parallel beyond available cores.', 'They eliminate the need for timeouts and bulkheads.', 'Every synchronized block permanently pins its virtual thread.', 'Virtual threads improve concurrency economics, not downstream capacity. Diagnose pinning and bound scarce dependencies.'],
  ['memory', 'JVM memory areas', 'Objects usually live on the heap, frames and local references on thread stacks, and class metadata in metaspace, with native memory used by several subsystems.', 'Every local object is allocated permanently on the stack.', 'Metaspace is part of the Java heap and always controlled by `-Xmx`.', 'A stable heap proves native memory cannot exhaust the container.', 'Container memory includes heap plus metaspace, code cache, thread stacks, direct buffers, GC structures, and other native allocations.'],
  ['gc-roots', 'GC roots and reachability', 'An object remains live while reachable from roots such as active thread stacks, static fields, JNI references, and JVM internals.', 'A reference cycle prevents collection even when unreachable from roots.', 'Calling `System.gc()` immediately frees every unused object.', 'Final fields are permanent GC roots.', 'Tracing collectors handle cycles. Memory leaks are unwanted reachability, so heap analysis follows paths from retained objects to roots.'],
  ['jit', 'JIT compilation', 'The JVM profiles hot code and may compile and optimize it, then deoptimize when assumptions fail.', 'All bytecode is fully optimized before `main` starts.', 'JIT removes the need for warm-up in benchmarks.', 'A method is compiled exactly once and cannot be deoptimized.', 'Tiered compilation, profiling, inlining, escape analysis, and deoptimization make warm-up and realistic JMH methodology important.'],
  ['classloading', 'class loading and delegation', 'Class loaders commonly delegate parent-first, and class identity includes both binary name and defining loader.', 'Two classes with the same name are always assignment-compatible.', 'Bootstrap loading discovers every application dependency from the classpath.', 'Class initialization occurs every time an instance is constructed.', 'Loading, linking, and initialization are distinct. Loader leaks and duplicate definitions matter in containers and plugin systems.'],
  ['records-sealed', 'records and sealed types', 'Records provide concise data carriers with component-based equality, while sealed types constrain permitted subtypes; neither automatically makes an object graph deeply immutable.', 'Records make every referenced collection immutable.', 'Sealed classes cannot contain methods or state.', 'Record components may be reassigned after construction.', 'Validate and defensively copy mutable components. Use sealed hierarchies when the closed subtype set improves domain modeling and exhaustive handling.'],
] as const;

