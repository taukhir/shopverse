# Synthetic identity evidence

- A controller unit test calls an admin method without the Spring Security filter chain.
- The UI hides the button for customers.
- Resource services cache JWKS while Auth Service replaces the signing key immediately.
- Internal Basic credentials are supplied by environment placeholders.
- A proposed debug log prints request headers and resolved configuration values.

No token, password, private key, hash, or real user record is included.
