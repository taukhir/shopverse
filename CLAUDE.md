@AGENTS.md

# Claude Code

- Load the nearest nested `CLAUDE.md` when working inside a service or the web
  application; each imports its scoped `AGENTS.md`.
- Use `/memory` to confirm the expected instruction files loaded when behavior
  appears inconsistent with repository guidance.
- Use plan mode for cross-service contracts, migrations, authorization, event
  schemas, or changes with unclear scope before editing.
