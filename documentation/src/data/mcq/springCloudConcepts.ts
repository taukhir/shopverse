import type {McqConcept} from './types';

export const springCloudConcepts: readonly McqConcept[] = [
  ['config-server', 'Spring Cloud Config Server', 'It centralizes external configuration from configured backends, while clients still need startup, authentication, versioning, and outage policies.', 'It distributes Java objects directly between service heaps.', 'It removes the need for secret management.', 'It guarantees every instance changes configuration atomically.', 'Treat configuration as a versioned dependency. Protect credentials, define fail-fast/retry behavior, and separate secrets when appropriate.'],
  ['refresh', 'runtime configuration refresh', 'Refreshing configuration can change selected bean/property state, but rollout consistency and behavior during mixed values must be designed.', 'Every property and singleton changes atomically cluster-wide.', 'Refresh automatically restarts database transactions safely.', 'Runtime refresh removes the need for deployment controls.', 'Prefer immutable rollout for risky settings. Audit changes and understand refresh scope, proxies, and partial-fleet behavior.'],
  ['discovery', 'service discovery', 'Discovery maps a logical service identity to healthy instances; clients or proxies still need selection, health, caching, and failure handling.', 'Discovery guarantees the chosen instance will stay alive for the request.', 'It replaces DNS, networking, and authorization.', 'Every service must run Eureka even on Kubernetes.', 'Choose registry/DNS/platform discovery from the environment. Stale registrations and caches are normal failure modes.'],
  ['loadbalancer', 'Spring Cloud LoadBalancer', 'It performs client-side instance selection from a supplied instance list and can apply policies, but does not create capacity or guarantee success.', 'It retries every failed mutation automatically.', 'It balances database queries inside a service transaction.', 'It makes all instances receive identical traffic permanently.', 'Selection, health, caching, retry and request hints affect distribution. Combine with deadlines and idempotency.'],
  ['gateway', 'Spring Cloud Gateway', 'A gateway centralizes edge routing and cross-cutting policy while owning reactive request/response flow and a potential blast radius.', 'It should contain all domain business logic.', 'It converts every downstream service into a synchronous monolith.', 'Gateway authentication removes authorization needs in services.', 'Keep filters bounded and non-blocking, enforce service ownership, and operate gateway capacity and failure independently.'],
  ['gateway-filters', 'gateway predicates and filters', 'Predicates decide whether a route matches; filters transform or enforce behavior before/after the proxied exchange.', 'A filter can safely block on JDBC in the reactive event loop.', 'Route order never matters when predicates overlap.', 'Response filters run before the downstream request is sent.', 'Trace ordering, body buffering, retries, error handling, and route precedence. Blocking work must be isolated or avoided.'],
  ['circuit-breaker', 'circuit breakers', 'A circuit breaker limits calls after observed failures; it does not replace timeouts, concurrency limits, or a correctness-aware fallback.', 'It prevents the first slow calls from consuming resources without a timeout.', 'It guarantees the downstream service has recovered when half-open starts.', 'One global breaker is ideal for every tenant and operation.', 'Choose meaningful failure classification and scope. Half-open probes and fallback behavior need monitoring and tests.'],
  ['retry', 'distributed retries', 'Retries should target transient, idempotent operations with exponential backoff, jitter, attempt/deadline budgets, and one clear ownership layer.', 'Every service layer should retry to maximize success.', 'Retrying immediately is safest during overload.', 'HTTP 500 proves a mutation did not happen.', 'Layered retries amplify load. Ambiguous mutations require stable identity, status lookup, or reconciliation.'],
  ['bulkhead', 'bulkheads', 'Bulkheads isolate concurrency or resources so one dependency, tenant, or workload cannot consume all capacity.', 'A bulkhead increases downstream capacity automatically.', 'An unbounded queue is the strongest bulkhead.', 'Bulkheads remove the need for load shedding.', 'Size isolation from capacity and SLOs. Saturation must fail or queue in a bounded, observable way.'],
  ['time-limiter', 'timeouts and deadline propagation', 'Each hop should receive a remaining end-to-end deadline with time reserved for response handling; cancellation support must be understood.', 'Every downstream timeout should exceed the caller timeout.', 'A client timeout guarantees server-side work stopped.', 'Timeouts make retries safe.', 'Timeouts bound waiting, not necessarily work. Propagate budgets and avoid orphaned expensive operations.'],
  ['openfeign', 'declarative HTTP clients', 'A declarative client reduces adapter boilerplate, but connection pools, codecs, errors, timeouts, retries, observability, and contracts still need explicit policy.', 'The interface declaration guarantees backward compatibility.', 'Feign calls are asynchronous and non-blocking by default.', 'A fallback may safely invent any business response.', 'Keep client DTOs/contracts owned, test error decoding, and do not hide remote failure semantics behind a local-looking method.'],
  ['binder', 'the Spring Cloud Stream binder', 'A binder connects application bindings to middleware-specific producers and consumers while broker semantics still matter.', 'It is an embedded message broker inside the application.', 'It guarantees broker independence for operations and delivery semantics.', 'It replaces Kafka partitions and consumer groups.', 'The programming model reduces API coupling, not distributed-system or broker-operational knowledge.'],
  ['functions', 'Spring Cloud Stream functional bindings', '`Supplier`, `Function`, and `Consumer` beans are discovered and connected through generated input/output binding names.', 'The Java bean name must equal the Kafka topic name.', 'A `Supplier` is invoked only after an HTTP request.', 'A `Consumer` automatically publishes its return value.', 'Binding names and destinations are distinct. Function definitions and composition determine what is bound.'],
  ['groups', 'consumer groups in Spring Cloud Stream', 'Different groups receive independent logical copies, while instances in one group share partitions/work.', 'Different business responsibilities should share one group to receive every message.', 'More instances always improve throughput regardless of partitions.', 'An anonymous group is the safest durable production subscription.', 'Assign distinct durable groups per responsibility. Effective parallelism and ordering remain constrained by partitions.'],
  ['partitioning', 'partitioning and ordering', 'A stable business key routes related events consistently and preserves ordering only within a partition, subject to retry and async processing choices.', 'Setting `partitionCount` guarantees global ordering.', 'Removing the key improves ordering across pods.', 'Consumer concurrency can process one partition in arbitrary order safely.', 'Choose keys from ordering and skew requirements. Increasing partitions can change mapping assumptions.'],
  ['streambridge', '`StreamBridge`', '`StreamBridge` supports imperative sends triggered by application logic; the target is a binding or dynamic destination governed by configuration.', 'It provides a database-and-broker atomic transaction automatically.', 'It should replace every `Function` return value.', 'A successful boolean proves the downstream consumer completed.', 'For critical database-to-message publication use outbox/CDC. Understand send acknowledgement and destination provisioning.'],
  ['errors', 'Spring Cloud Stream retry and DLQ handling', 'Retry and DLQ behavior combines common binding, binder-specific, and native client settings, with explicit ownership and replay policy.', 'Enabling a DLQ guarantees malformed messages are corrected.', 'Infinite in-memory retry best preserves throughput.', 'Retry topics always preserve strict original ordering.', 'Classify transient/permanent failures, bound retry, monitor age/rate, preserve metadata, and control replay.'],
  ['observability', 'messaging observation and tracing', 'Metrics, logs, and trace context should expose binding, destination, group, partition, offset, attempts, latency, and business correlation without leaking sensitive data.', 'A trace proves an event was processed exactly once.', 'Consumer lag alone identifies the root cause.', 'Putting access tokens in headers improves correlation.', 'Use telemetry as evidence across framework and broker boundaries. Reconciliation and invariants prove business correctness.'],
  ['kubernetes', 'Spring Cloud components on Kubernetes', 'Kubernetes DNS, ConfigMaps/Secrets, Services, and probes may replace some Spring Cloud infrastructure, but behavior and operational trade-offs must be evaluated.', 'Kubernetes automatically replaces every gateway, circuit breaker, and config policy.', 'Eureka is mandatory for Kubernetes Service routing.', 'ConfigMaps provide secret encryption and rotation by default.', 'Avoid duplicate control planes without a need. Platform primitives do not erase application resilience and security requirements.'],
  ['compatibility', 'Spring Cloud release compatibility', 'Spring Boot, Spring Cloud release train, binder, and underlying client versions must be selected from supported compatibility guidance and tested together.', 'The newest independent version of every module is always compatible.', 'Maven resolves semantic incompatibility automatically.', 'Forcing a Kafka client version cannot affect binder behavior.', 'Use BOMs and official compatibility matrices, then test configuration and behavior during upgrades.'],
] as const;

