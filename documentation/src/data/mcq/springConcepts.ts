import type {McqConcept} from './types';

export const springConcepts: readonly McqConcept[] = [
  ['ioc-di', 'IoC and dependency injection', 'The container creates and wires components from definitions, making object construction and dependency policy external to business code.', 'Dependency injection requires field injection on every bean.', 'IoC means Spring owns all domain decisions.', 'A bean obtains dependencies by calling the application context in each method.', 'Constructor injection exposes required dependencies, supports immutability and testing, and avoids service-locator coupling.'],
  ['lifecycle', 'the Spring bean lifecycle', 'Definition loading, instantiation, dependency population, aware callbacks, post-processing, initialization, use, and destruction are distinct lifecycle phases.', '`@PostConstruct` runs before dependency injection.', 'Bean post-processors can only run after application startup completes.', 'Destruction callbacks run for every prototype bean automatically.', 'Post-processors enable proxies and annotation handling. Prototype destruction is normally the caller’s responsibility.'],
  ['scopes', 'Spring bean scopes', 'Singleton is per application context; request/session scopes require an active web scope and often a proxy when injected into longer-lived beans.', 'Singleton means one instance across every JVM and cluster.', 'Prototype beans injected into singletons are recreated on every method call automatically.', 'Request-scoped state is safe in background threads without context propagation.', 'Scope mismatch requires a provider/proxy or redesign. Singleton beans must not hold request-specific mutable state.'],
  ['auto-config', 'Spring Boot auto-configuration', 'Auto-configuration contributes beans when classpath, properties, existing beans, and other conditions match.', 'It scans every dependency and instantiates every configuration unconditionally.', 'It replaces all user beans after context refresh.', 'It only works when component scanning finds the auto-configuration package.', 'Boot imports registered auto-configurations and applies conditions; the condition report is key diagnostic evidence.'],
  ['backoff', 'auto-configuration back-off', 'Many auto-configurations back off at a specific bean or capability boundary when application configuration supplies an alternative.', 'Declaring any bean disables all Spring Boot auto-configuration.', 'Back-off means Boot deletes the custom bean and restores its default.', 'Back-off behavior is identical across every auto-configuration.', 'Inspect the exact conditional annotation and bean type/name. Customizing one boundary may leave adjacent defaults active.'],
  ['aop-proxy', 'Spring AOP proxies and self-invocation', 'Proxy advice applies when a call crosses the proxy; direct self-invocation normally bypasses proxy-based transaction, cache, async, or security advice.', 'Private methods are automatically intercepted through subclass proxies.', 'Static methods are advised because they belong to the class.', 'Adding an annotation forces bytecode rewriting by default.', 'Move the boundary to another proxied bean, call through an injected proxy only when justified, use a programmatic template, or choose AspectJ weaving deliberately.'],
  ['tx-propagation', 'transaction propagation', '`REQUIRED` joins or creates a transaction, while `REQUIRES_NEW` suspends an existing one and consumes an independent transaction/connection.', '`REQUIRES_NEW` rolls back automatically when the outer transaction rolls back.', '`SUPPORTS` always creates a new transaction.', '`NESTED` behaves identically on every transaction manager.', 'Propagation changes atomicity, connection demand, and failure behavior. Nested savepoints depend on manager and resource support.'],
  ['tx-isolation', 'transaction isolation in Spring', 'Spring requests an isolation level from the transaction manager/database; the engine defines actual anomalies, locking, MVCC, and support.', 'Spring emulates serializable isolation in application memory.', 'Isolation eliminates every distributed-system race.', '`READ_UNCOMMITTED` is always faster and therefore preferred.', 'Choose from invariants and real engine behavior. Isolation does not cover remote APIs or separate databases.'],
  ['dispatcher', '`DispatcherServlet` request processing', 'It coordinates handler mapping, adapter invocation, argument resolution, return handling, view/body writing, and exception resolution.', 'It performs TCP connection acceptance directly.', 'It runs before the servlet filter chain.', 'It creates a new application context for each request.', 'The servlet container and filters precede DispatcherServlet. MVC strategies turn the selected handler into a response.'],
  ['filter-interceptor', 'filters versus MVC interceptors', 'Filters operate at the servlet boundary; interceptors operate around mapped MVC handlers and do not replace security filters.', 'Interceptors run before every servlet filter.', 'Filters can access the selected controller method before handler mapping.', 'Security is safest when implemented only in controller interceptors.', 'Choose the extension point from ownership and timing. SecurityFilterChain is built on servlet filters.'],
  ['exceptions', 'Spring MVC exception handling', '`@ExceptionHandler` and `@ControllerAdvice` translate exceptions at the web boundary, while business exceptions should preserve domain meaning and causes.', 'Every exception should return HTTP 200 with an error payload.', 'Catching exceptions in each controller is required for rollback.', 'A global handler can recover a transaction after it has committed.', 'Map stable error contracts without leaking internals. Transaction rollback occurs at the transactional boundary, not in response serialization.'],
  ['validation', 'Bean Validation in Spring MVC', '`@Valid`/`@Validated` triggers constraint validation at supported boundaries; validation does not replace authorization or database invariants.', 'Validation annotations sanitize all malicious strings automatically.', 'Controller validation guarantees the database cannot be changed concurrently.', 'Method validation works without any proxy or infrastructure.', 'Use validation for shape and rules at boundaries, then enforce ownership, concurrency, and persistence constraints where they belong.'],
  ['security-chain', '`SecurityFilterChain` matching and order', 'The first matching chain is used, so matchers and order must be explicit and a fallback chain should protect unmatched requests.', 'All matching chains merge their authorization rules.', 'A chain declared later always overrides earlier chains.', 'Public endpoints should omit security filters entirely.', 'Unexpected access often comes from chain matching/order or an overly broad permit rule. Test positive and negative routes.'],
  ['csrf', 'CSRF protection', 'CSRF matters when browsers automatically attach credentials such as cookies; stateless bearer-token APIs have a different threat model.', 'CORS replaces CSRF protection.', 'CSRF only affects GET requests.', 'Disabling sessions automatically prevents browser credential attachment.', 'Select protection from credential transport and client architecture. SameSite, CSRF tokens, origin controls, and XSS defenses address different risks.'],
  ['cors', 'CORS', 'CORS is a browser-enforced policy controlling which origins may read/use cross-origin responses; it is not server authentication.', 'CORS prevents non-browser clients from calling an API.', 'Using `*` with credentials is a secure production default.', 'CORS verifies that the user owns the requested resource.', 'Configure exact origins, methods, headers, and credentials policy. Authorization remains server-side.'],
  ['persistence-context', 'the JPA persistence context', 'It provides identity mapping and tracks managed entities within its lifecycle; it is not a distributed cache.', 'A managed entity is automatically shared safely across request threads.', 'Closing the persistence context commits every pending database transaction.', 'Detached entities continue dirty checking automatically.', 'Entity state and transaction state are related but distinct. Long contexts increase staleness and memory risk.'],
  ['dirty-flush', 'dirty checking and flush', 'Hibernate compares managed state and synchronizes changes at flush; flush may occur before commit or queries and does not itself guarantee commit.', 'Dirty checking updates every table row on every transaction.', '`save()` always issues SQL immediately.', '`flush()` permanently commits changes.', 'Understand flush mode, generated SQL, batching, transaction rollback, and database constraints rather than equating repository calls with wire activity.'],
  ['fetching', 'JPA fetching and the N+1 problem', 'Fetch plans should match each use case; projections, entity graphs, batch fetching, joins, and two-step pagination have different duplication and paging trade-offs.', 'Changing every association to EAGER prevents N+1 safely.', 'Joining multiple to-many associations never multiplies rows.', 'Pagination is always correct with a collection fetch join.', 'Measure SQL count, rows, memory, and query plan. Avoid a universal mapping-level fetch policy.'],
  ['test-context', 'Spring test slices and context caching', 'Slices load focused infrastructure, while context caching reuses compatible configurations; unnecessary mocks/properties can create costly unique contexts.', '`@SpringBootTest` is always faster because it uses production configuration.', 'A mocked bean never changes the cached context key.', 'Transactional test rollback proves production commit behavior.', 'Choose unit, slice, and full integration tests by boundary. Test real commit, concurrency, and engine behavior where correctness depends on them.'],
  ['config-actuator', 'configuration properties and Actuator', 'Typed configuration validates external settings, while Actuator exposes controlled health, metrics, and diagnostics; sensitive endpoints require deliberate exposure.', 'All Actuator endpoints should be public for platform probes.', '`@Value` provides stronger grouped validation than typed properties.', 'A health endpoint should run every deep downstream query on each probe.', 'Separate liveness, readiness, and detailed diagnostics. Sanitize configuration and secure operational endpoints.'],
] as const;

